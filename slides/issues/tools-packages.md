## Nuget Issues
### Tools packages awkward

Packages are installed into project and require at least one content-file or libs

* Cannot create packages eg. with msbuild scripts
* Result: weird packages with `delete-me.txt` files, etc.

This seems to have partially changed in latest Nuget versions